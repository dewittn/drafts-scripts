authorUpdate:
  dateFormat: "%B Y"
  templateSettings:
    draftTags:
      - newsletter
      - drafting
    templateFile: "cp/innocent-dreams-newsletter.md"
    templateTag: "publish_date"
    workspaceName: "defualt"
    nextAction: "Add Draft to Pipeline"
  uiSettings:
    getMonthFromPrompt:
      menuSettings:
        menuTitle: Author Update
        menuMessage: When are you sending out this update?
        menuItems: []
beyondTheBook:
  templateSettings:
    draftTags:
      - beyond the book
      - drafting
    templateFile: "cp/beyond-the-book.md"
    workspaceName: "defualt"
    templateTag: "working_title"
    nextAction: "Add Draft to Pipeline"
  uiSettings:
    getTitleFromPrompt:
      menuSettings:
        menuTitle: Beyond The Book
        menuMessage: "What is your working title (no markdown):"
        isCancellable: false
        menuItems:
          - type: button
            data:
              name: Go
              value: go
              isDefault: true
          - type: textField
            data:
              name: workingTitle
              label: ""
              initialText: ""
